@model HealthRecordsPro.Models.AcademicSemesterPeriod

@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Create</h1>

<h4>AcademicSemesterPeriod</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="PeriodTypeId" class="control-label"></label>
                <select asp-for="PeriodTypeId" class ="form-control" asp-items="ViewBag.PeriodTypeId"></select>
            </div>
            <div class="form-group">
                <label asp-for="AcademicSemesterId" class="control-label"></label>
                <select asp-for="AcademicSemesterId" class ="form-control" asp-items="ViewBag.AcademicSemesterId"></select>
            </div>
            <div class="form-group">
                <label asp-for="RecordedBy" class="control-label"></label>
                <select asp-for="RecordedBy" class ="form-control" asp-items="ViewBag.RecordedBy"></select>
            </div>
            <div class="form-group">
                <label asp-for="DateRecorded" class="control-label"></label>
                <input asp-for="DateRecorded" class="form-control" />
                <span asp-validation-for="DateRecorded" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
